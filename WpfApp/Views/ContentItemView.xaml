<UserControl x:Class="WpfApp.Views.ContentItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:Microsoft.Toolkit.Wpf.UI.Controls;assembly=Microsoft.Toolkit.Wpf.UI.Controls"
             xmlns:local="clr-namespace:WpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="5">
        <Label Content="Name: " />
        <TextBox Text="{Binding SelectedItem.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Emoji: " />
        <TextBox Text="{Binding SelectedItem.Emoji, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <controls:InkToolbar TargetInkCanvas="{x:Reference inkCanvas}" />
        <controls:InkCanvas x:Name="inkCanvas" Height="500" />
    </StackPanel>
</UserControl>
